#ifndef CORE
#define CORE
                                
#include <vector>
#include <string>

void save(const std::vector<std::string>& file);
std::string getfile_ext(const std::string& filename);

#endif 
